name: CI - Java & Python Build and Test

on: # Eventos que activan el workflow
  push:
    branches:
      - main
      - dev
      - feature

env: # Variables de entorno
  SONAR_TOKEN: ${{ secrets.MAIN_TOKEN }} # Token for main branch
  JAVA_SRC: src/com/example/sqlinjectiontest
  PYTHON_DIR: python
  RESULTS_DIR: results

jobs: # Conjunto de las tareas
  build-test: # Nombre de la tarea (job)
    runs-on: ubuntu-latest # Virtualiza en Ubuntu

    steps: # Pasos de la tarea
    - name: 📥 Checkout code
      uses: actions/checkout@v4

    # ------------ JAVA BUILD & TEST ------------
    # - name: ☕ Set up JDK 17 # Instala y configura Java - TEMURIN
    #   uses: actions/setup-java@v4
    #   with:
    #     distribution: 'temurin'
    #     java-version: '17'

    - name: ☕ Setting JDK 17... # Instala y configura Java - ZULU
      uses: actions/setup-java@v2
      with:
        java-version: '17' 
        distribution: 'zulu'

    - name: 🛠️ Compile Java code # Compila el código de Java
      run: |
        mkdir -p ${{ env.RESULTS_DIR }}/java
        javac ${{ env.JAVA_SRC }}/Main.java -d ${{ env.RESULTS_DIR }}/java/

    - name: 🚀 Run Java main (if it has a main method)
      run: | # Envia los resultados a la ruta results/ y los almacena en un .txt
        cd ${{ env.RESULTS_DIR }}/java
        java com.example.sqlinjectiontest.Main | tee ../../${{ env.RESULTS_DIR }}/java/java-output.txt || true

    # ------------ PYTHON TESTS ------------
    - name: 🐍 Python config. # Instala y configura Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: 📦 Install dependencies for Python # Instala las librerias de Python a usar
      run: |
        pip install pytest flake8 bandit black

    - name: ✏️ Check code styles (flake8) # Ejecuta flake8
      run: |
        mkdir -p ${{ env.RESULTS_DIR }}/python
        flake8 python/ | tee ${{ env.RESULTS_DIR }}/python/pytest-output.txt
      continue-on-error: true
  
    - name: 📊 Check code formatting (black) # Ejecuta black
      run: black --check python/ | tee ${{ env.RESULTS_DIR }}/python/pytest-output.txt
      continue-on-error: true

    - name: 🕵🏻‍♂️ Run security analysis (bandit) # Ejecuta bandit en busca de vulnerabilidades
      run: bandit -r python/ -x tests/ -s B101 | tee ${{ env.RESULTS_DIR }}/python/pytest-output.txt # Ignora el assert de python (-x tests/ -s B101)
      continue-on-error: true

    - name: 🧪 Run pytests # Ejecuta los pytests
      run: |
        mkdir -p ${{ env.RESULTS_DIR }}/python
        pytest -s -v --disable-warnings python/file.py | tee ${{ env.RESULTS_DIR }}/python/pytest-output.txt
      continue-on-error: true
      
    # ------------ ARTIFACTS ------------
    - name: 📁 Upload test results # Almacena los resultados de las pruebas
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: results/

    # ------------ SUMMARY ------------
    - name: 📋 Summary # Resumen de las tareas de Java y Python
      run: |
        echo "✅ Java and Python tasks completed."
        echo "📄 Outputs stored in 'results/' and uploaded as artifacts."

    - name: 📦 Install Sonar Scanner
      run: |
        wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
        unzip sonar-scanner-cli-5.0.1.3006-linux.zip
        mv sonar-scanner-5.0.1.3006-linux sonar-scanner
        echo "$(pwd)/sonar-scanner/bin" >> $GITHUB_PATH

    - name: ☑️ Verify SonarQube...
      run: |
        curl -I http://rnghg-31-4-242-208.a.free.pinggy.link || true

    - name: ▶️ Run SonarQube scan
      run: |
        sonar-scanner \
          -Dsonar.projectKey=VariosTests \
          -Dsonar.host.url=http://rnghg-31-4-242-208.a.free.pinggy.link \
          -Dsonar.token=${{ secrets.MAIN_TOKEN }} \
          -Dsonar.sources=python,src \
          -Dsonar.python.version=3.x \
          -Dsonar.qualitygate.wait=true \
          -X
    # ------------ OPTIONAL: Docker (commented) ------------
    # - name: 🐳 Run docker-compose (optional)
    #   run: docker-compose up -d
